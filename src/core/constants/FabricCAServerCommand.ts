/**
 * @enum FabricCAServerCommand
 * @description Enum representing the main commands available in `fabric-ca-server` CLI.
 */
export enum FabricCAServerCommand {
  COMPLETION = "completion",
  HELP = "help",
  INIT = "init",
  START = "start",
  VERSION = "version",
}
